# Public project identity. prepare_project.py writes .env with the selected name.
PROJECT_NAME=example
# Optional project-specific defaults; shared defaults come from setup/docker/.env.
# PROFILE=prestashop
# PHP_VERSION=8.1
# PrestaShop debug: off, on, or ip (requires comma-separated PS_DEBUG_IPS).
# PS_DEBUG_MODE=off
# PS_DEBUG_IPS=
