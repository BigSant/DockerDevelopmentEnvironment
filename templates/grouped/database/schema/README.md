# Schema snapshots

Run make schema-export, review and git add the files, then make schema-check.
The optional make schema-hook-install applies to the repository containing
this directory, not to a separate application repository.
