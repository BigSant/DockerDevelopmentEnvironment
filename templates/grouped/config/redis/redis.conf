bind 0.0.0.0
# Reachable by application containers on network_app; do not publish port 6379.
protected-mode no
port 6379
daemonize no
logfile ""
dir /data

# Compose mounts only the chosen environment at this fixed location.
include /usr/local/etc/redis/environment/redis.conf
