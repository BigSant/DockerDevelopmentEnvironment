# Analysis baselines

Commit reviewed baseline files here. The initial PHPStan baseline is empty;
it does not assert that the application passes analysis. Generate explicitly
with make phpstan-baseline, then review and stage the output.
