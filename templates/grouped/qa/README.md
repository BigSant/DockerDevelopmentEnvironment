# Quality tools

Run make phpstan, make phpcs and make e2e from the Docker directory.
Tool configs live here; outputs use the project data directory. Run make init
to prepare writable cache/report folders. Build optional tool images explicitly.
