version: "3"
services:

  nginx-proxy:
    container_name: ${PROJECT_NAME?}-${ENV?}-nginx-proxy
    restart: unless-stopped
    build:
      context: .
      dockerfile: ./docker/nginx-proxy/Dockerfile
      args:
        ENV: ${ENV?}
        DOMAIN: ${DOMAIN?}
        WHITELISTED_IP: ${WHITELISTED_IP}
#    volumes:
#      - ./_cache/nginx/ssl/certs/:/etc/nginx/ssl/certs/
#      - ./_cache/nginx/ssl/private/:/etc/nginx/ssl/private/
    ports:
      - ${LOCALHOST_PORT}:80
      - ${LOCALHOST_PORT_SSL}:443
    networks:
      - network_app

  mariadb:
    container_name: ${PROJECT_NAME?}-${ENV?}-mariadb
    restart: unless-stopped
    build:
      context: .
      dockerfile: ./docker/mariadb/Dockerfile
      args:
        DATABASE_HOST: ${DATABASE_HOST:-%}
        DATABASE_USER: ${DATABASE_USER?}
        DATABASE_PASSWORD: ${DATABASE_PASSWORD?}
        DATABASE_NAME: ${DATABASE_NAME?}
        EXPORTER_PASSWORD: ${EXPORTER_PASSWORD?}
    env_file:
      - ${ENV_FILE?}
    environment:
      MYSQL_ROOT_HOST: ${DATABASE_HOST:-%}
      MYSQL_ROOT_PASSWORD: ${DATABASE_PASSWORD?}
      MYSQL_USER: ${DATABASE_USER?}
      MYSQL_PASSWORD: ${DATABASE_PASSWORD?}
      MYSQL_DATABASE: ${DATABASE_NAME?}
    volumes:
      - ./_cache/mariadb/${PROJECT_NAME?}-${ENV?}:/var/lib/mysql
    depends_on:
      - nginx-proxy
    networks:
      - network_app
    healthcheck:
      test: "mariadb-admin ping -h 127.0.0.1 -u${DATABASE_USER} -p${DATABASE_PASSWORD} --silent"
      retries: 3
      interval: 5s

  php-fpm-base:
    container_name: ${PROJECT_NAME}-${ENV}-php-fpm-base
    restart: unless-stopped
    build:
      context: .
      dockerfile: ./docker/php-fpm/${PHP_VERSION?}/Dockerfile
      target: env-${ENV?}
      args:
        PHP_VERSION: ${PHP_VERSION?}
    profiles:
      - build_only
    env_file:
      - ${ENV_FILE?}
    networks:
      - network_app

  php-fpm:
    container_name: ${PROJECT_NAME}-${ENV}-php-fpm
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4096M
    build:
      context: .
      dockerfile: ./Dockerfile
      target: env-${ENV?}
      args:
        ENV: ${ENV?}
        PROJECT_NAME: ${PROJECT_NAME?}
    env_file:
      - ${ENV_FILE?}
#    environment:
#      PS_DEV_MODE: 1
#      DB_SERVER: ${DATABASE_HOST}
#      DB_USER: ${DATABASE_USER}
#      DB_PASSWD: ${DATABASE_PASSWORD}
#      DB_NAME: ${DATABASE_NAME}
#      PS_DOMAIN: ${DOMAIN}
#      PS_FOLDER_ADMIN: admintestas
#      XDEBUG_CONFIG: remote_host=host.docker.internal
    depends_on:
      - nginx-proxy
      - mariadb
    networks:
      - network_app
    volumes:
      - ./app/:/var/www/html/
#    healthcheck:
#      test: ['CMD', 'curl', '-f', 'localhost:80']
#      interval: 10s
#      start_period: 15s
#      timeout: 10s
#      retries: 5

  nginx:
    container_name: ${PROJECT_NAME}-${ENV}-nginx
    restart: unless-stopped
    build:
      context: .
      dockerfile: ./docker/nginx/Dockerfile
      args:
        DOMAIN: ${DOMAIN?}
    env_file:
      - ${ENV_FILE?}
    depends_on:
      - nginx-proxy
      - php-fpm
    networks:
      - network_app
    volumes:
      - ./app/:/var/www/html/

#  certbot:
#    container_name: ${PROJECT_NAME}-${ENV}-certbot
#    restart: unless-stopped
#    build:
#      context: .
#      dockerfile: ./docker/certbot/Dockerfile
#      target: env-${ENV?}
#      args:
#        DOMAIN: ${DOMAIN?}
#    env_file:
#      - ${ENV_FILE?}
#    depends_on:
#      - nginx-proxy
#    networks:
#      - network_app
#    volumes:
#      - ./_cache/certbot/letsencrypt:/etc/letsencrypt
#      - ./_cache/certbot/www:/tmp/letsencrypt

  pma:
    container_name: ${PROJECT_NAME}-${ENV}-pma
    restart: unless-stopped
    build:
      context: .
      dockerfile: ./docker/pma/Dockerfile
      target: env-${ENV?}
      args:
        DATABASE_USER: ${DATABASE_USER?}
        DATABASE_PASSWORD: ${DATABASE_PASSWORD?}
    env_file:
      - ${ENV_FILE?}
    environment:
      PMA_HOST: mariadb
    depends_on:
      - mariadb
    networks:
      - network_app


#
#  php-fmp:
#    container_name: ${PROJECT_NAME}-${ENV}-php
#    restart: unless-stopped
#    tty: true
#    build:
#      context: .
#      dockerfile: conf/php/8.1/Dockerfile
#    networks:
#      - network_app
#    volumes:
#      # APP mapping
#      - ./app/:/var/www/html/
##      - ./_cache/php/log:/var/log/fpm-php.www.log



  mailpit:
    container_name: ${PROJECT_NAME}-${ENV}-mailpit
    restart: unless-stopped
    image: axllent/mailpit
    env_file:
      - ${ENV_FILE?}
    environment:
      TZ: Europe/Vilnius
      MP_MAX_MESSAGES: 5000
      MP_UI_AUTH_FILE: /data/authfile
      MP_DATA_FILE: /data/mailpit.db
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
    networks:
      - network_app
    depends_on:
      - nginx-proxy
    volumes:
      - ./_cache/mailpit:/data

  grafana:
    image: grafana/grafana-oss:10.4.0
    container_name: ${PROJECT_NAME}-${ENV}-grafana
    restart: unless-stopped
    networks:
      - network_app
    volumes:
      - ./_cache/grafana:/var/lib/grafana

  prometheus:
    container_name: ${PROJECT_NAME}-${ENV}-prometheus
    restart: unless-stopped
    build:
      context: .
      dockerfile: ./docker/prometheus/Dockerfile
    depends_on:
      - grafana
    networks:
      - network_app
    volumes:
      - ./_cache/prometheus:/prometheus

  mysqld-exporter:
    container_name: ${PROJECT_NAME?}-${ENV?}-mysqld-exporter
    restart: unless-stopped
    build:
      context: .
      dockerfile: ./docker/mysqld-exporter/Dockerfile
      args:
        EXPORTER_PASSWORD: ${EXPORTER_PASSWORD?}
    env_file:
      - ${ENV_FILE?}
    depends_on:
      - grafana
      - mariadb
    networks:
      - network_app




#  nginx-prometheus-exporter:
#    image: nginx/nginx-prometheus-exporter:0.10.0
#    container_name: ${PROJECT_NAME}-${ENV}-nginx-prometheus-exporter
#    restart: unless-stopped
#    command:
#      - -nginx.scrape-uri=http://nginx/metrics
#      - -web.telemetry-path=/metrics
#    networks:
#      - network_app
#    depends_on:
#      - prometheus
#      - nginx

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.47.2
    container_name: ${PROJECT_NAME}-${ENV}-cadvisor
    restart: unless-stopped
    depends_on:
      - grafana
    networks:
      - network_app
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro

#  loki:
#    image: grafana/loki:2.9.2
#    container_name: ${PROJECT_NAME}-${ENV}-loki
#    restart: unless-stopped
#    depends_on:
#      - grafana
#    command: -config.file=/etc/loki/local-config.yaml
#    networks:
#      - network_app
#
#  promtail:
#    image: grafana/promtail:2.9.2
#    container_name: ${PROJECT_NAME}-${ENV}-promtail
#    restart: unless-stopped
#    depends_on:
#      - loki
#    command: -config.file=/etc/promtail/config.yml
#    networks:
#      - network_app

networks:
  network_app:
    driver: bridge
    name: ${PROJECT_NAME}-${ENV}-network
