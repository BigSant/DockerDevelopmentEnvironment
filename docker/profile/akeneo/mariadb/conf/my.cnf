[mysqld]
# Akeneo-specific MariaDB tuning — loaded as conf.d/zz-profile.cnf (overrides base my.cnf).
# Active only when PROFILE=akeneo.
#
# Akeneo needs higher limits for large catalogs / exports, e.g.:
#group_concat_max_len = 1000000
